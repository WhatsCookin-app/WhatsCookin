import React, {useEffect, useRef, useState} from 'react'
import {propTypes} from 'react-bootstrap/esm/Image'

export let roomId

const Room = props => {
  roomId = props.roomId
  return null
}

export default Room
